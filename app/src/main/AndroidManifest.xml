<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ozi.ukm">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ukm"
        android:usesCleartextTraffic="true">
        <activity android:name=".profilee"></activity>
        <activity android:name=".dialogg" />
        <activity android:name=".profile" />
        <activity android:name=".ui.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".detail" />
        <activity android:name=".splashscreen" />
        <activity android:name=".freash" />
        <activity android:name=".homeact" />
        <activity android:name=".lihatsemua_adapter" />
        <activity android:name=".lihat_kategori" />
        <activity android:name=".kategori" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>